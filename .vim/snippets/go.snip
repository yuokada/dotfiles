snippet     for
abbr        for ...() { ... }
options     head
  for ${1} = 0; $1 < ${2}; $1++ {
      ${0:TARGET:return }
  }


snippet     fori
abbr        infinity for { ... }
options     head
  for {
      ${0:TARGET:return }
  }

snippet     switch
abbr        switch () {}
    switch ${1:#:var} {
    case ${2:#:val}:
         ${0:TARGET}
    }

snippet    case
abbr       case (condition)
    case ${1:#:val}:
        ${0:TARGET}

